<%= form_for @order, :url => user_orders_path(@user) do |o| %>
  
   <%= o.text_field :family_type%>
   <%= o.text_field :delivery_address%>
   <%= o.datetime_local_field :delivery_date %>

   <%= o.submit %>
<% end %>