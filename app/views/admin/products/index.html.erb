<p>This is product index page</p>
<table>
		
			<tr><% @products.each do |p| %></tr>
		 	  <td><%= p.name %></td>
		  	<td><%= link_to 'Show', admin_product_path(p)%></td>
		  	<td><%= link_to 'Edit', edit_admin_product_path(p)%></td>
		  	<td><%= link_to 'Delete',admin_product_path(p), :method => :delete%></td>
			<% end %>
			
</table>

</br>
<%= link_to 'Add New Product', new_admin_product_path  %>
</br>
<%= link_to '所有使用者', admin_users_path %> 